#!/usr/bin/python3 -I
"""Observational host TLS verification; never issue, publish or reload."""
import os
import sys

os.environ.clear()
os.environ.update({"PATH": "/usr/sbin:/usr/bin:/sbin:/bin", "LANG": "C", "LC_ALL": "C", "HOME": "/"})
sys.path.insert(0, "/usr/local/lib/homelab-tls")
from tls_runtime.runtime import main

sys.exit(main("verify"))
