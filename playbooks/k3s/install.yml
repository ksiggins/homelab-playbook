- hosts: k3s_cluster
  become: true
  roles:
    - role: grog.package
      package_list: [
        name: qemu-guest-agent,
        name: xterm
      ]
    - geerlingguy.security
    - geerlingguy.docker

- hosts: k3s_cluster
  roles:
    - xanmanning.k3s

- hosts: all
  roles:
    - k3s/kubeconfig
