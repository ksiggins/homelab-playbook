---
- hosts: all
  connection: local
  gather_facts: false
  roles:
    - ssh/scankeys

- hosts: all
  become: true
  roles:
    - role: grog.package
      package_list: [
        name: qemu-guest-agent,
        name: xterm
      ]
    - geerlingguy.security
